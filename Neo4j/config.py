"""
행동 인식 시스템의 설정 파라미터
"""

# 가우시안 분포 파라미터
GAUSSIAN_CONFIG = {
    # 최소 표준편차 (낮을수록 더 엄격한 범위)
    "MIN_SIGMA": 0.15, # 매우 좁은 범위만 높은 점수
    
    # 신뢰구간 시그마 배수 (낮을수록 더 좁은 범위)
    # 1.0: 68% 신뢰구간
    # 2.0: 95% 신뢰구간
    # 3.0: 99.7% 신뢰구간
    "CONFIDENCE_SIGMA": 0.7,  # 검색 범위
    
    # 학습률 (높을수록 새로운 데이터의 영향이 커짐)
    "LEARNING_RATE": 0.05, # 새로운 데이터의 영향 작게
    
    # 유사도 임계값 (높을수록 더 엄격한 매칭)
    "SIMILARITY_THRESHOLD": 0.07
}

# 각도 범위 제한
ANGLE_CONFIG = {
    "MIN_ANGLE": 0,
    "MAX_ANGLE": 360
}
